# efficiency — Token-Saving & Prompt Efficiency Skills

**Status:** ❌ Not yet installed

## TODO
- Find or create efficiency/prompt skills repo
- Install to `~/.claude/skills/efficiency/`

## Suggested Efficiency Skills to Build
- `/compress` — compress long prompts without losing context
- `/context-trim` — trim context before session limit
- `/batch` — batch multiple small tasks into one call
- `/template` — reusable prompt templates per task type
